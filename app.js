(()=>{const app=document.querySelector('#app'),api=async(p,o={})=>{const r=await fetch('/api/'+p,{...o,headers:{'content-type':'application/json',...(o.headers||{})}}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Request failed.');return d},esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let me,deps,roles,types,statuses;
function login(){app.innerHTML=`<section class="login"><h1 class="brand">Broad Concept Hub</h1><h2>Staff Request Portal</h2><p class="muted">Sign in with your staff account.</p><form id="lf"><label>Username<input name="username" required></label><label>Password<input name="password" type="password" required></label><p class="error" id="lm"></p><button>Sign in</button></form></section>`;lf.onsubmit=async e=>{e.preventDefault();try{await api('login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(lf)))});start()}catch(x){lm.textContent=x.message}}}
async function start(){let d=await api('me');me=d.user;roles=d.roles;types=d.types;statuses=d.statuses;deps=(await api('departments')).items;app.innerHTML=`<div class="shell"><aside class="side"><h2>Broad Concept</h2><p>${esc(me.name)}<br><small>${esc(me.role)}</small></p><button data-page="home">Dashboard</button><button data-page="new">New request</button><button data-page="requests">Requests</button>${['Administrator','Head of Operations'].includes(me.role)?'<button data-page="monitor">Monitoring</button>':''}${me.role==='Administrator'?'<button data-page="admin">Administration</button>':''}<button id="logout">Sign out</button></aside><section class="content" id="content"></section></div>`;document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>page(b.dataset.page));logout.onclick=async()=>{await fetch('/api/logout');login()};page('home')}
async function page(name){if(name==='home'){let r=(await api('requests')).items,open=r.filter(x=>!['Completed','Declined'].includes(x.status)),late=open.filter(x=>x.expected_date&&new Date(x.expected_date)<new Date());content.innerHTML=`<h1>Dashboard</h1><div class="grid"><div class="card"><h2>${r.length}</h2><p>Accessible requests</p></div><div class="card"><h2>${open.length}</h2><p>Open requests</p></div><div class="card"><h2>${late.length}</h2><p>Overdue requests</p></div></div><div class="card"><h2>Start here</h2><p>Use Administration to add staff accounts. Staff can then sign in and submit requests.</p></div>`}if(name==='new'){content.innerHTML=`<h1>New request</h1><div class="card"><form id="newform"><label>Request type<select name="type">${types.map(x=>`<option>${x}</option>`).join('')}</select></label><label>Department<select name="department_id">${deps.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select></label><label>Title<input name="title" required></label><label>Description<textarea name="description" required></textarea></label><label>Expected completion date<input name="expected_date" type="date"></label><p class="error" id="newmsg"></p><button>Submit request</button></form></div>`;newform.onsubmit=async e=>{e.preventDefault();try{await api('requests',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(newform)))});page('requests')}catch(x){newmsg.textContent=x.message}}}if(['requests','monitor'].includes(name)){let r=(await api('requests')).items;if(name==='monitor')r=r.filter(x=>!['Completed','Declined'].includes(x.status));content.innerHTML=`<h1>${name==='monitor'?'Operational monitoring':'Requests'}</h1><div class="card"><input id="search" placeholder="Search requests"><div id="requestlist"></div></div>`;const draw=()=>requestlist.innerHTML=r.filter(x=>JSON.stringify(x).toLowerCase().includes(search.value.toLowerCase())).map(x=>`<div class="card row"><div><strong>${esc(x.title)}</strong><br><span class="muted">${esc(x.type)} · ${esc(x.department)} · ${esc(x.requester)}</span></div><div><span class="tag">${esc(x.status)}</span><br><button onclick="window.openRequest(${x.id})">Open</button></div></div>`).join('')||'<p class="muted">No requests found.</p>';search.oninput=draw;draw()}if(name==='admin'){let staff=(await api('staff')).items;content.innerHTML=`<h1>Administration</h1><div class="grid"><div class="card"><h2>Add staff member</h2><form id="staffform"><label>Full name<input name="name" required></label><label>Username<input name="username" required></label><label>Temporary password<input name="password" type="password" minlength="12" required></label><label>Role<select name="role">${roles.map(x=>`<option>${x}</option>`).join('')}</select></label><label>Department<select name="department_id">${deps.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select></label><p class="error" id="staffmsg"></p><button>Add staff member</button></form></div><div class="card"><h2>Staff accounts</h2>${staff.map(x=>`<p><strong>${esc(x.name)}</strong><br><span class="muted">${esc(x.username)} · ${esc(x.role)} · ${esc(x.department||'No department')}</span></p>`).join('')}</div></div>`;staffform.onsubmit=async e=>{e.preventDefault();try{await api('staff',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(staffform)))});page('admin')}catch(x){staffmsg.textContent=x.message}}}}
window.openRequest=async id=>{let d=await api('requests/'+id),r=d.request,can=['Administrator','Head of Operations'].includes(me.role)||r.assigned_role===me.role;content.innerHTML=`<button onclick="window.page('requests')">Back</button><h1>${esc(r.title)}</h1><div class="card"><p><span class="tag">${esc(r.status)}</span> ${esc(r.type)}</p><p>${esc(r.description)}</p><p class="muted">Department: ${esc(r.department_id)} · Assigned to: ${esc(r.assigned_role)}</p></div>${can?`<div class="card"><h2>Update request</h2><form id="updateform"><select name="status">${statuses.map(x=>`<option ${x===r.status?'selected':''}>${x}</option>`).join('')}</select><textarea name="comment" placeholder="Add a comment. A comment is required to decline."></textarea><button>Update status</button></form>${r.type==='Leave or Absence'&&me.role==='Main Instructor'?'<button id="forward">Forward to Head of Operations</button>':''}</div>`:''}<div class="card"><h2>Comments</h2>${d.comments.map(x=>`<p><strong>${esc(x.author)}</strong><br>${esc(x.body)}</p>`).join('')||'<p class="muted">No comments yet.</p>'}<form id="commentform"><textarea name="body" required placeholder="Add comment"></textarea><button>Add comment</button></form></div><div class="card"><h2>Activity</h2>${d.activity.map(x=>`<p>${esc(x.created_at)} · ${esc(x.actor||'System')} · ${esc(x.action)} ${esc(x.details||'')}</p>`).join('')}</div>`;updateform&&(updateform.onsubmit=async e=>{e.preventDefault();try{await api('requests/'+id+'/status',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(updateform)))});openRequest(id)}catch(x){alert(x.message)}});commentform.onsubmit=async e=>{e.preventDefault();await api('requests/'+id+'/comments',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(commentform)))});openRequest(id)};forward&&(forward.onclick=async()=>{await api('requests/'+id+'/forward',{method:'POST',body:'{}'});openRequest(id)})};window.page=page;start().catch(login)})();
